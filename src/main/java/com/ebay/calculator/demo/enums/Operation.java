package com.ebay.calculator.demo.enums;

/**
 * Enum representing basic arithmetic operations. Each operation is implemented
 * as a constant with an associated behavior defined in the {@code apply} method.
 * This enum supports operations such as addition, subtraction, multiplication, and division.
 */
public enum Operation {
    ADD,
    SUBTRACT,
    MULTIPLY,
    DIVIDE
}
